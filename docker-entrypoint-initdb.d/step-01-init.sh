#!/bin/bash
set -e

psql -v ON_ERROR_STOP=1 --username "$POSTGRES_USER" --dbname "$POSTGRES_DB"
<<-E0SQL
  CREATE USER "$DB_USER" PASSWORD '$DB_PASSWORD';
  CREATE DATABASE films;
  GRANT ALL PRIVILEGES ON DATABASE films TO "$DB_USER";
E0SQL